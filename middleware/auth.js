// export default function  {
//     // If the user is not authenticated
//     const currentUser =  this.$parse.User.current()
//         if (currentUser) {
//           console.log('Usuário está logado:',  currentUser.getUsername(), currentUser)
//         } else {
//           console.log('Usuário não está logado', currentUser)
//           if(!currentUser)
//         return redirect('/register');
//         }
//   }

export default function ({redirect}) {

    console.log("teste")
    // const currentUser = await this.$parse.User.current()
    // if (currentUser != null) {
    //   console.log('Usuário está logado:', await currentUser.getUsername(), currentUser)
    // } 

    // if(currentUser === null){
    //   console.log('Usuário não está logado', currentUser)
    //     return redirect('/register');
    // }
  }